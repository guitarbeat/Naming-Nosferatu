# Palette's Journal

## 2025-02-04 - Icon-only Button Accessibility
**Learning:** Icon-only buttons (like those in Lightbox) are invisible to screen readers without explicit labels, creating a barrier for non-visual users.
**Action:** Always verify icon-only interactive elements include `aria-label` or `aria-labelledby` during implementation.
