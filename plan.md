1. **Understand**
   - The CI is failing again at `pnpm install --frozen-lockfile`.
   - Error: `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH Cannot proceed with the frozen installation. The current "overrides" configuration doesn't match the value found in the lockfile`.
   - The issue might be that I used `pnpm config set lockfile true` or similar when I pushed, but in GitHub actions, `pnpm/action-setup@v4` has a specific configuration.
   - Wait, `pnpm install --no-frozen-lockfile` generated a lockfile, but maybe I should have just run `pnpm install` without args when creating the lockfile?
   - Let's look at `package.json` -> `pnpm` section:
     ```json
     "pnpm": {
         "overrides": {
           "@isaacs/brace-expansion": ">=5.0.1",
           "tar@<=7.5.2": ">=7.5.3",
           "tar@<=7.5.3": ">=7.5.4"
         }
     }
     ```
   - Wait, there's `pnpm-workspace.yaml` which ALSO has overrides:
     ```yaml
     overrides:
       tar@<=7.5.2: '>=7.5.3'
       tar@<=7.5.3: '>=7.5.4'
       undici@<5.29.0: '>=5.29.0'
       undici@<6.23.0: '>=6.23.0'
       undici@>=4.5.0 <5.28.5: '>=5.28.5'
     ```
   - Ah! Having overrides in BOTH `package.json` and `pnpm-workspace.yaml` is known to cause `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` because pnpm merges them inconsistently or complains when comparing the lockfile against the two sources.
   - Or maybe the lockfile format generated by pnpm v10.28.0 (locally) doesn't match pnpm v10.27.0 (which CI uses). Let's install pnpm 10.27.0 and regenerate the lockfile!

2. **Action**
   - Install pnpm v10.27.0: `npm install -g pnpm@10.27.0`
   - Run `pnpm install --no-frozen-lockfile`
   - Commit the updated `pnpm-lock.yaml` and push again.

3. **Verify**
   - Check `pnpm install --frozen-lockfile` works with v10.27.0.
