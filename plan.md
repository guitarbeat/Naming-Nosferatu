<thinking>
1. **Consolidate Dashboard views with SegmentedControl**: Use `replace_with_git_merge_diff` to update `src/features/dashboard/Dashboard.tsx` to replace the 2 split buttons with the `SegmentedControl` from `src/shared/components/ui/SegmentedControl.tsx`. Use `run_in_bash_session` to execute `git diff src/features/dashboard/Dashboard.tsx` to verify the changes.
2. **Consolidate Admin Tabs with SegmentedControl**: Use `replace_with_git_merge_diff` to update `src/features/dashboard/components/admin/components/AdminTabNav.tsx` to utilize `SegmentedControl` instead of manual mapping of buttons to unify tab patterns. Use `run_in_bash_session` to execute `git diff src/features/dashboard/components/admin/components/AdminTabNav.tsx` to verify the changes.
3. **Fix SegmentedControl Accessibility**: Use `replace_with_git_merge_diff` on `src/shared/components/ui/SegmentedControl.tsx` to correctly apply accessibility standards from memory (`role="tablist"` on container, `role="tab"` on items, `aria-selected` tracking). Use `run_in_bash_session` to execute `git diff src/shared/components/ui/SegmentedControl.tsx` to verify the changes.
4. **Clean up HomeRoute Navigation**: Use `replace_with_git_merge_diff` on `src/app/routes/HomeRoute.tsx` to remove scattered `Button` components for back/next and rely on the existing, unified `SectionNavigation` component. Use `run_in_bash_session` to execute `git diff src/app/routes/HomeRoute.tsx` to verify the changes.
5. **Add MagicToggle to NameSelector**: Use `write_file` to create `src/shared/components/ui/MagicToggle.tsx` with a playfully animated framer-motion toggle. Use `replace_with_git_merge_diff` to integrate it into `src/features/tournament/components/NameSelector.tsx` replacing the raw button toggle for `hiddenShowSelectedOnly`. Use `run_in_bash_session` to execute `git diff src/features/tournament/components/NameSelector.tsx` and `ls -l src/shared/components/ui/MagicToggle.tsx` to verify the changes.
6. **Testing**: Use `run_in_bash_session` to execute `pnpm test run` to ensure the UI changes do not cause tests to fail.
7. **Pre-commit**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
</thinking>
