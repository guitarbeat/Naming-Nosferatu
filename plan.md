1. Use `replace_with_git_merge_diff` to refactor raw buttons in `src/features/tournament/modes/TournamentFlow.tsx` to use the `Button` component from `components/layout/Button.tsx`.
2. Use `replace_with_git_merge_diff` to refactor raw buttons in `src/features/tournament/components/TournamentComplete.tsx` to use the `Button` component.
3. Use `replace_with_git_merge_diff` to refactor raw buttons in `src/features/tournament/components/MatchSideCard.tsx` to use the `Button` component.
4. Use `replace_with_git_merge_diff` to refactor raw buttons in `src/features/tournament/components/TournamentHeader.tsx` to use the `Button` component.
5. Use `run_in_bash_session` to run the full test suite and verifications (`pnpm run check:maintenance && pnpm run lint:types && pnpm test`).
6. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
7. Use the `submit` tool to submit the PR.
