🎯 **What:** The haptics utilities (`src/shared/lib/browser/haptics.ts`) were entirely untested. These simple functions wrap `navigator.vibrate` calls and are easily mocked. I have added a test suite to ensure they correctly interact with the `navigator` API and gracefully handle environments where it is missing. I also resolved unrelated formatting/unused variable issues flagged by the linter as part of pre-commit steps.

📊 **Coverage:**
- Happy path for `hapticNavTap` to ensure `navigator.vibrate` is called with `10`.
- Happy path for `hapticTournamentStart` to ensure `navigator.vibrate` is called with `[50, 50, 50]`.
- Edge cases testing environment safety (does not throw if `navigator` or `navigator.vibrate` are undefined).

✨ **Result:** Test coverage for `src/shared/lib/browser/haptics.ts` is now at 100% across all lines, functions, and branches. Overall codebase coverage and reliability are improved.
